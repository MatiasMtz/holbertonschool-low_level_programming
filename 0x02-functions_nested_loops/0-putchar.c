#include <stdio.h>
#include <string.h>
#define  LENGTH 80
/**
 * main - Entry point.
 * Putchar - writes a character to stdout.
 * Return: 0 if success.
 */
int main(void)
{
	int i, ch;
	char buffer[LENGTH + 1] = "_putchar\n";

	return (0);
}
