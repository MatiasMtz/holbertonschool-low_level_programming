#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Responsible for the execution and termination of the program.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char l;
for (l = 'a'; l >= 'a'; l--)
{
putchar(l);
}
putchar('\n');
return (0);
}
